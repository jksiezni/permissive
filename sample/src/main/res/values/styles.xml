<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AskInContextDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:background">#ff9800</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="EducateInContextDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:background">#8bc34a</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>
</resources>
